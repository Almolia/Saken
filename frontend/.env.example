# Optional. Leave empty for auto-detection.
# Local: defaults to current page origin with backend on port 8000.
# GitHub Codespaces: auto-converts the current frontend URL (port 5173) to backend URL (port 8000).
VITE_API_BASE_URL=
